---
title: FlexPooling with Simple Auxiliary Classifiers in Deep Networks
title_zh: 深度网络中结合简单辅助分类器的FlexPooling
authors: "Muhammad Ali, Omar Alsuwaidi, Salman Khan"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14926v1"
tags: ["query:deepfake-fsl"]
score: 8.0
evidence: 提出一种新的池化方法和辅助分类器以提升CNN分类性能。
tldr: "在卷积神经网络中，下采样池化对于扩大感受野、减少参数和计算量不可或缺，然而它是一种有损压缩，必须尽可能保留最具判别力的激活以提升模型判别力。但常见的最大池化或平均池化使用固定规则，不能自适应地选择关键信息。本文提出FlexPooling，一种通过学习加权平均来泛化平均池化的方法，将权重学习融入网络训练，自动聚焦显著激活；同时加入简易辅助分类器（SAC）增强特征表示。实验表明，在多个图像分类基准上，FlexPooling一致超越标准池化，取得了约1%至3%的准确率提升，验证了这种轻量自适应池化的效益与即插即用的便利性，为池化设计提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 737, \"height\": 520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14926v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1010, \"height\": 424, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14926v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 948, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14926v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 621, \"height\": 440, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14926v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 324, \"label\": \"Table\"}]"
motivation: 标准池化（最大/平均）采用固定规则，无法自适应保留判别性激活，限制了CNN特征提取效果。
method: 提出FlexPooling，一种学习加权平均的自适应池化，与网络联合优化；并附加简单辅助分类器(SAC)增强特征表示。
result: 在多个主流图像分类数据集上，FlexPooling一致优于标准池化基线，准确率提升约1~3个百分点。
conclusion: FlexPooling作为一种轻量级自适应池化，简单有效，配合SAC能稳定提升CNN性能，具有即插即用的通用性。
---

## 摘要
在计算机视觉中，大多数卷积神经网络的基本流程由多个特征提取层组成，其中输入信号在每一层被下采样到更低分辨率。这个下采样过程通常称为池化，是CNN中的基本操作。池化提高了对变换的鲁棒性，减少了可训练参数的数量，增大了感受野，并降低了计算时间。由于池化是一个有损过程，但对于从低级表示中提取高级信息仍然重要，因此需要保留先前激活中最显著的信息以提高网络的判别能力。标准池化通常使用密集池化方法，如最大池化或平均池化，或通过跨步卷积核。在本文中，我们提出了一种简单而有效的自适应池化方法，称为FlexPooling，它通过与网络其余部分联合学习激活上的加权平均来推广平均池化。我们进一步表明，将简单辅助分类器（SAC）附加到CNN可以提高性能，并证明了与标准池化方法相比所提出方法的有效性。在多个流行的图像分类数据集上的实验表明，FlexPooling始终优于基线网络，精度提高约1%到3%。

## Abstract
In computer vision, the basic pipeline of most convolutional neural networks consists of multiple feature extraction layers, where the input signal is downsampled to a lower resolution in each subsequent layer. This downsampling process is commonly referred to as pooling, which is an essential operation in CNNs. Pooling improves robustness against transformations, reduces the number of trainable parameters, increases the receptive field, and lowers computation time. Since pooling is a lossy process but remains important for extracting high-level information from low-level representations, it is important to preserve the most prominent information from previous activations to improve network discriminability. Standard pooling is usually performed using dense pooling methods, such as max pooling or average pooling, or through strided convolutional kernels. In this paper, we propose a simple yet effective adaptive pooling method, called FlexPooling, which generalizes average pooling by learning a weighted average over activations jointly with the rest of the network. We further show that attaching Simple Auxiliary Classifiers (SAC) to the CNN improves performance and demonstrates the effectiveness of the proposed method compared with standard pooling methods. Experiments on multiple popular image classification datasets show that FlexPooling consistently outperforms baseline networks, achieving approximately 1 to 3 percent improvement in accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **研究背景**：在卷积神经网络中，池化是一个关键环节，主要用于下采样以减少计算量与参数量，并扩大感受野，使得模型能够提取高层次的信息。但池化本质上是一个有损过程且有固定偏向，因此如何有效池化对于保留关键信息、提升模型判别力至关重要。
*   **核心问题**：传统的池化方法，例如平均池化和最大池化，均基于预设的假设且不可学习，因此无法自适应地保留最具判别力的特征。平均池化假设局部均值具有充分代表性，而最大池化则仅取局部最大响应。这些固定规则无法在训练中调整，可能会丢失对任务至关重要的空间细节，不利于模型泛化。
*   **整体含义**：本文旨在解决现有池化层不可学习的局限，提出一种简单有效的自适应池化方法，使池化操作能通过与网络联合训练，动态学习如何加权保留重要信息，从而提升模型在图像分类任务上的准确性。

### 2. 论文提出的方法论
*   **核心思想**：提出 **FlexPooling**，一种可训练的池化方法。它通过学习特征图上的加权平均，来泛化传统的全局平均池化，让网络能自动聚焦于对分类更关键的空间区域。此外，引入 **简单辅助分类器 (SAC)**，在网络的多个中间阶段进行监督，以增强梯度流动，帮助网络学习更具判别力的特征表示。
*   **关键技术细节与流程**：
    *   **FlexPooling 块**：在网络的最后一个卷积阶段，初始化一个与输出特征图尺寸相同的可学习参数块 \( w_{fp} \)，其初始值设为与传统全局平均池化相同，即每个元素为 \( \frac{1}{高度 \times 宽度} \)。
    - **加权平均运算**：将FlexPooling块与输入特征图进行逐元素点乘，然后按高度和宽度维度求和，生成最终的加权平均输出向量，送入后续的分类头。
    - **正则化 FlexPool**：为确保参数学习后仍符合加权平均的语义，引入正则化项 \( R(FlexPool) \)。该正则化使用均方误差 (MSE) 损失，约束每个通道中所有可学习权重的和为1：\[\displaystyle R(FlexPool) = \sum_{k=1}^{C} \left( \sum_{i=1}^{N} \sum_{j=1}^{N} w_{fp_{i,j,k}} - 1 \right)^2\]
    *   **FlexPooling + SAC**：将不同深度的卷积块输出的特征图，分别通过独立的FlexPooling块压缩为一维向量，并分别计算交叉熵损失。最终的训练损失是这些不同阶段损失值的加权和，定义为：\[Loss = \lambda_1 L(h_1(x), y) + \lambda_2 L(h_2(h_1(x)), y) + \lambda_3 L(h_3(h_2(h_1(x))), y)\]其中 \( L \) 为分类交叉熵损失，权重通常设定为 \( \lambda_1 < \lambda_2 < \lambda_3 \)（如 0.1, 0.2, 0.7），使得后期更抽象的特征对训练影响更大。

### 3. 实验设计
*   **数据集与场景**：论文在四个标准的图像分类基准数据集上进行了评估。
    *   **低尺度数据集**：CIFAR-10, CIFAR-100, Fashion-MNIST。
    *   **中尺度数据集**：Tiny ImageNet。
*   **基准模型**：使用 ResNet-20 作为所有实验的主干网络。
*   **对比方法**：文中系统对比了多种池化策略的变体，以验证不同组件的作用。
    *   **基线**：标准全局平均池化 (AvgPool)。
    *   **单一分类器设置**：
        *   FlexPool
        *   FlexPool + 正则化 (Reg)
        *   FlexPool + 正则化 + Dropout
    *   **多分类器设置 (FlexPool + SAC)**：
        *   FlexPool
        *   FlexPool + 正则化 (Reg)
        *   FlexPool + 正则化 + Dropout

### 4. 资源与算力
*   **算力资源**：论文提到，模型训练使用一块 **NVIDIA A100 GPU** 完成。
*   **训练时长与配置**：在FlexPooling方法中，模型训练 **100个epoch**，batch size设置为1000。在SAC设置中，分为两个阶段：首先训练10个epoch，然后再训练100个epoch。优化器使用 **ADAM**，初始学习率为0.1，并采用单周期学习率（One Cycle LR）策略。

### 5. 实验数量与充分性
*   **实验数量**：论文进行了较为详尽的实验。
    *   在**4个**不同规模的数据集上进行主实验。
    *   在每个数据集上，分别测试了两种主要的分类器设置：**单一分类器**和**多分类器 (SAC)**。
    *   在每种设置下，又测试了**4种**池化变体（AvgPool, FlexPool, FlexPool+Reg, FlexPool+Reg+Dropout）。
    *   综合来看，总共包含了 **\(4 \times 2 \times 4 = 32\)** 组以上的核心对比实验（以表中数据计）。
*   **充分性与公平性**：实验设计很扎实。通过在多个标准数据集上使用统一的主干网络进行横向对比，确保了结果的代表性。消融研究设计得清晰，从基本FlexPool到增广正则化和Dropout，再到多分类器结构，层层递进地验证了每个模块的有效性。对比基线明确，公平性较高，结果呈现出的稳定提升趋势具有说服力。

### 6. 论文的主要结论与发现
*   FlexPooling作为一种可学习池化方法，其性能在所有测试数据集和设置下均优于标准全局平均池化。
*   引入正则化项约束权重的和为1，以及添加Dropout，能进一步增强FlexPooling的性能和泛化能力，通常带来额外的准确率提升。
*   结合简单辅助分类器（SAC），从网络中间层获取监督信号，能有效改善梯度流动和信息传递，使得FlexPooling+SAC的设置获得了最高的准确率提升，例如在Tiny ImageNet上带来了接近**3.5%** 的显著增益。
*   该方法原理简单、效果稳定，可作为一种即插即用的插件，用于替换现有先进CNN架构中的池化层以期获得性能提升。

### 7. 优点
*   **方法简洁有效**：FlexPooling的思想非常直观，通过引入少量可学习参数，将固定的平均池化转变为自适应的加权平均，实现成本低但收益明显。
*   **通用性与即插即用**：该方法不依赖于特定的网络架构（文中以ResNet-20为例，但原理可通用），可以直接替换任何CNN中的全局池化层。
*   **实验说服力强**：在多个不同类型的基准数据集上进行了评估，并详细设计了消融实验，严谨地证明了所提出方法及其各个组件（正则化、Dropout、SAC）的作用，结论可靠。
*   **细节考虑周全**：对可学习池化块施加“权重和为1”的正则化约束，是本文的一个亮点，它在赋予池化自适应能力的同时，保留了其作为加权平均的物理含义，有助于训练的稳定性。

### 8. 不足与局限
*   **模型覆盖有限**：实验仅使用了较为轻量的分类网络 ResNet-20，未在部署于其他更复杂任务上的更大型、结构差异更大的现代架构上进行验证。
*   **任务范围单一**：所有验证均仅在图像分类任务上进行。论文未探讨FlexPooling在目标检测、语义分割或实例分割等其他视觉任务中是否同样有效，其通用性在这些任务上仍有待考证。
*   **应用偏差风险**：FlexPooling学习的是一组与特征图层同等大小的权重，如果训练数据存在分布偏差，权重的学习可能会过拟合于训练集的特定空间激活模式，导致在真实场景下的泛化能力下降，虽然消融实验中的正则化设计可在一定程度上缓解此问题。
*   **算力需求细节缺失**：未明确提及完整的训练时长（小时/天），而这对于评估方法在大规模数据上的实际成本是有用的参考信息。

（完）
