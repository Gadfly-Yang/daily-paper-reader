---
title: "When Confidence Lacks Concepts: Interpretable OOD Detection via Representation Perturbations"
title_zh: 当置信度缺乏概念支撑：基于表征扰动的可解释分布外检测
authors: "Anju Chhetri, Pratik Shrestha, Ramesh Rana, Prashnna Gyawali, Binod Bhattarai"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16196v1"
tags: ["query:deepfake-fsl"]
score: 8.0
evidence: 利用表示扰动和稀疏自编码器的可解释OOD检测方法，可用于检测生成图像
tldr: 深度模型在医学影像任务中表现优异，但分布偏移下易过泛化，现有OOD检测方法缺乏可解释性，限制临床信任。本文提出基于类条件语义扰动的可解释OOD检测框架，用稀疏自编码器学习类概念向量，通过测量预测稳定性区分分布内与OOD样本。实验表明内分布样本扰动敏感性低，OOD偏差大，有效提供判别信号与内部机制解释。该方法提升医学AI部署安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16196v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1249, \"height\": 842, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16196v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1235, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16196v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1256, \"height\": 829, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16196v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1151, \"height\": 622, \"label\": \"Table\"}]"
motivation: 现有OOD检测方法依赖不透明内部信号，缺乏语义解释，限制了医学安全关键应用中的信任。
method: 利用稀疏自编码器从分布内数据学习类特定概念向量，扰动深层表示并测量logits稳定性实现可解释OOD检测。
result: 分布内样本对类条件扰动敏感性低，OOD样本偏差显著，有效判别并提供内部表征对齐差异的解释。
conclusion: 该方法通过语义扰动分析提供可判别的OOD信号，增强模型可解释性与医学AI部署安全性。
---

## 摘要
深度神经网络在医学成像任务中取得了卓越的性能，但它们在分布变化下过度泛化的倾向对安全临床部署构成了重大障碍。分布外（OOD）检测方法旨在降低这种风险，但大多数现有方法依赖于语义意义理解不清的不透明内部信号，限制了在安全关键场景中的信任。在本工作中，我们提出一个可解释的OOD检测框架，该框架探究模型预测在类别条件语义扰动下的稳定性。我们利用稀疏自编码器（SAEs），从分布内数据中学习类别特定的概念向量，将稠密中间表征解耦为稀疏、语义上有意义的成分。在推理时，我们使用与模型预测类别相关联的概念向量扰动深层表征，并测量类别logits的稳定性。我们假设，分布内样本对此类扰动表现出低敏感性，因为它们的表征与类别特定的语义方向一致；而OOD样本则由于表征错位而显示出放大的偏差。通过将OOD检测构建为基于概念条件的稳定性分析，我们的方法既提供了判别性的OOD信号，又为驱动模型不确定性的内部机制提供了可解释的视角，使其特别适用于高风险的医学应用。

## Abstract
Deep neural networks have achieved remarkable performance across medical imaging tasks, yet their tendency to overgeneralize under distributional shifts poses a major obstacle to safe clinical deployment. Out-of-Distribution (OOD) detection methods aim to mitigate this risk, but most existing approaches rely on opaque internal signals with poorly understood semantic meaning, limiting trust in safety-critical settings. In this work, we propose an interpretable OOD detection framework that probes the stability of model predictions under class-conditioned semantic perturbations. Leveraging sparse autoencoders (SAEs), we learn class-specific concept vectors from in-distribution data that disentangle dense intermediate representations into sparse, semantically meaningful components. At inference, we perturb deeper-layer representations using the concept vectors associated with the model's predicted class and measure the class logits stability. We hypothesize that in-distribution samples exhibit low sensitivity to such perturbations, as their representations align with class-specific semantic directions, whereas OOD samples show amplified deviations due to representational misalignment. By framing OOD detection as a concept conditioned stability analysis, our approach provides both a discriminative OOD signal and an interpretable lens into the internal mechanisms driving model uncertainty, making it particularly suitable for high stakes medical applications.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：深度神经网络在医学影像任务上性能卓越，但在发生分布偏移（如不同设备、人群或新的病理类型）时会过度泛化，产生不可靠的预测，给临床安全部署带来重大风险。
- **核心问题**：现有的分布外（Out-of-Distribution, OOD）检测方法大多依赖语义含义不明的内部信号（如最大 softmax 概率、能量分数、特征空间距离），虽然能够发现异常样本，却无法解释检测决策背后的语义依据或模型内部机制。
- **整体含义**：该工作旨在提出一种可解释的 OOD 检测框架，将模型预测的稳定性与类别特定的语义概念联系起来，从而在给出判别性 OOD 信号的同时，提供对模型不确定性内在原因的理解，特别适合医学高风险场景。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用稀疏自编码器（SAE）从分布内（ID）数据学习类别特定的概念方向（概念向量），并在推理时用这些概念向量扰动深层表征，测量预测类 logit 的稳定性。ID 样本与对应概念方向高度对齐，对扰动敏感度低；OOD 样本则表征错位，logit 变化大，据此实现 OOD 检测。
- **关键技术细节**：
  - **SAE 训练**：以 Vision Transformer（ViT）的中间层 [CLS] token 激活 \(h\) 为输入，训练 SAE 进行稀疏重构，损失函数为 \(L = \|h - \bar{h}\|_2^2 + \gamma \|s(h)\|_1\)，得到稀疏编码和对应的解码器列向量（概念向量）。
  - **类别特定概念提取**：对每个类别 \(c\)，计算训练样本的平均稀疏激活 \(\mu_c\)，选取 top-\(k\)（默认 \(k=20\)）响应最强的潜在维度，将其对应的解码器列组成类别概念字典 \(K_c\)。
  - **类别条件表征扰动**：给定测试样本 \(\bar{x}\)，由基础模型预测类别 \(\hat{y}\)，取出其对应的概念字典 \(K_{\hat{y}}\)，将其中概念向量加和并做 \(l_2\) 归一化，得到单一类别概念向量 \(v_{\hat{y}}\)。在最终层对 [CLS] 表征施加扰动 \(\tilde{h}^l = h^l + \beta v_{\hat{y}}\)（\(\beta\) 控制扰动强度），前向传播后计算预测类别 logit 变化量 \(\Delta \ell_c\)。
  - **OOD 评分**：\(\Delta \ell_c\) 作为 OOD 检测统计量，当 \(\Delta \ell_c > \tau\) 时判为 OOD（\(\tau\) 于 ID 验证集上按 95% 真阳性率确定）。
  - **理论支撑（Lemma 1）**：在一阶泰勒近似下，logit 变化 \(\Delta \ell_c \approx \beta \mathbf{w}_c^\top J_\phi(h) v_c\)；对 ViT 中的 LayerNorm 给出显式形式，包含表征与概念方向的对齐项 \((h-\mu(h))^\top v_c\)，揭示 ID 样本对齐更强、扰动更低。

### 3. 实验设计
- **数据集与场景**：
  - **Kvasir-v2**（内窥镜图像）：3 个健康解剖标志作为 ID，其余 5 个类别作为 OOD。
  - **Histopathology**（NCT-CRC-HE-100K 和 CRC-VAL-HE-7K，结直肠癌组织病理）：癌相关间质（STR）与肿瘤上皮（TUM）作为 OOD，其余为 ID。
  - **Retinal OCT**（视网膜光学相干断层扫描）：Normal 与 CNV 作为 ID，DME 与 DRUSEN 作为 OOD（检测未见过的视网膜病变）。
- **对比方法**：Energy、Entropy、MSP、Mahalanobis、Vim、NECO、MaxLogit、ODIN、Energy+React、NERO、GradNorm、I-ODIN 等主流 OOD 检测基线。
- **评价指标**：AUROC（越高越好）和 FPR95％（越低越好）。

### 4. 资源与算力
- 论文未提供具体的 GPU 型号、数量或训练时长等信息，并注明“Full implementation and training details will be provided in the released code.”（完整的实现和训练细节将在发布的代码中提供）。因此，算力消耗情况在当前版本中不可知。

### 5. 实验数量与充分性
- **主实验**：在三个不同医学成像模态（内窥镜、组织病理、视网膜 OCT）上，与 12 种基线方法进行 AUROC 和 FPR95 的比较（表 1），覆盖了多种 OOD 检测范式。
- **超参数鲁棒性分析**：针对概念数量 \(k\) 和扰动强度 \(\beta\) 在三个数据集上进行参数扫描（图 2(b)），评估方法的稳定范围。
- **表象对齐实验**：展示了 ID 与 OOD 样本激活与概念向量的余弦相似度差异（图 2(a)），支撑类-表征对齐的假设。
- **定性临床验证**：由盲法临床专家对 SAE 概念对应的最高激活图像及低激活 OOD 样本进行视觉语义评估（图 3），验证概念的可解释性与 OOD 缺失情况。
- 总体而言，实验在三个差异显著的数据集上开展，基线全面，消融分析包含关键超参数，设计较为充分、对比公平。但对数据极度匮乏的 Kvasir-v2 场景存在性能波动，体现出受数据量限制的局限。

### 6. 论文的主要结论与发现
- 所提方法（CAPS）在 OCT 和 Histopathology 上取得了最优 AUROC（93.67％ 和 95.28％）及最低 FPR95（24.80％ 和 23.17％），在 Kvasir-v2 上性能中等（AUROC 79.58％），归因于训练数据有限导致语义概念质量不足。
- ID 样本在概念条件扰动下 logit 变化小，证实了其表征与类别概念方向良好对齐；OOD 样本则变化显著，可作为稳健的 OOD 判别信号。
- 临床专家验证表明 SAE 学到的潜在维度对应着可识别的临床标志（如幽门口、Z 线、阑尾开口、胆汁反流等），而 OOD 样本即使被模型高置信度错误分类，其对应概念活性很低，即预测缺乏可解释的特征支撑。

### 7. 优点（方法或实验设计的亮点）
- **可解释性与检测的统一**：首次将基于概念的表征扰动融入 OOD 检测，既提供判别分数，又揭示模型“为什么”不确定，通过概念缺失解释异常。
- **方法无痛后处理**：无需重新训练基础模型，仅需额外训练 SAE 并提取概念向量，实现即插即用的 OOD 检测。
- **跨模态泛化**：在三个解剖部位和成像技术迥异的医学数据集上验证，显示了良好的领域适应潜力。
- **严密的理论描述**：给出 LayerNorm 下的 logit 灵敏度闭式解，将 OOD 行为与表示对齐联系起来。
- **临床专家背靠背定性评估**：为模型可解释性提供了人为判断的实质性证据，增强了可信度。

### 8. 不足与局限
- **低数据场景性能不稳定**：在训练样本较少的 Kvasir-v2 上，SAE 难以学到丰富、判别的类别概念，导致 OOD 检测性能下降且对超参数较敏感。
- **概念字典大小固定**：所有类别统一使用 top-\(k\)（如 20）个概念，未根据不同类别语义复杂性进行自适应调整。
- **对 SAE 训练质量的依赖**：SAE 本身是无监督训练的，概念质量受数据分布、预训练质量和超参数影响，缺乏确保概念可解释性的内在保证。
- **未涉及其他架构**：实验仅基于 ViT 模型（ViT-S/16 或 ViT-B/16），在其他网络结构（如 CNN、Swin Transformer）上的适用性未知。
- **计算开销方面未量化**：SAE 训练和推理中额外的概念注入及前向传播带来的计算成本未与基线对比分析。
- **潜在的临床应用壁垒**：临床部署需对 SAE 概念字典进行持续维护，当 ID 数据分布演变时可能需要重新学习。

（完）
