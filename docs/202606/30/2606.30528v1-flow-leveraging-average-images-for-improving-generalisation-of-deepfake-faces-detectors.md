---
title: "$μ$Flow: Leveraging Average Images for Improving Generalisation of Deepfake Faces Detectors"
title_zh: "$μ$Flow: 利用平均图像提升深度伪造人脸检测器泛化能力"
authors: "Orazio Pontorno, Mattia Litrico, Luca Guarnera, Mario Valerio Giuffrida, Sebastiano Battiato"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30528v1"
tags: ["query:deepfake-fsl"]
score: 9.0
evidence: 单类深度伪造检测器仅用真实图像训练，利用平均图像放大生成痕迹，提升对GAN和扩散模型的跨域泛化
tldr: 生成模型日益逼真，但现有深度伪造检测器依赖监督训练，跨生成器泛化受限。μFlow提出仅用真实图像训练，通过平均多张图像放大生成痕迹，利用归一化流将单图特征对齐至平均特征分布，获得高判别力的似然准则。在完全分布外设置下，μFlow显著超越SOTA检测器，展示了无需伪影即可实现强泛化的能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30528v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 629, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30528v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1260, \"height\": 606, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30528v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1262, \"height\": 521, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30528v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1265, \"height\": 357, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30528v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 513, \"height\": 318, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30528v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 1065, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30528v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1269, \"height\": 491, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30528v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 615, \"height\": 386, \"label\": \"Table\"}]"
motivation: 现有检测器依赖真假图像监督训练，限制了跨生成器类别（如GAN与DM）的泛化。
method: 利用平均图像放大生成痕迹，通过归一化流将单图特征对齐至平均特征分布，仅用真实图像训练。
result: 在完全分布外评估中，μFlow大幅优于最先进检测器，泛化性能显著提升。
conclusion: μFlow作为一类检测器，无需伪影即可通过特征对齐实现强泛化，性能大幅领先。
---

## 摘要
当前的生成模型，包括 GAN 和扩散模型，已达到极高的逼真度，对隐私和安全构成重大风险。为确保实际应用，深度伪造检测器必须能有效泛化到未见过的生成器。然而，大多数现有方法依赖于同时使用真实和伪造图像的有监督训练，这限制了其泛化能力，尤其是在跨生成器类别（如 GAN 与 DM）时。本文提出 $μ$Flow，一种仅使用真实图像训练的单类深度伪造检测器，无需依赖伪深度伪造或合成 artifacts。我们的方法基于一个观察：对多张图像求平均会放大一致的生成痕迹，从而产生高度区分的特征表示。我们利用这一特性，对从平均图像中提取的特征分布进行建模，并训练一个归一化流，使单张图像的特征空间向该分布对齐。这种对齐产生了一种基于似然的准则，可分离真假样本，同时促进强泛化。我们在完全分布外设置下评估 $μ$Flow，即训练时未见任何真实或伪造数据集。实验结果表明，我们的方法显著优于当前最先进的检测器。项目页面: https://opontorno.github.io/MuFlow。

## Abstract
Current generative models, including GANs and diffusion models, have reached an outstanding level of photorealism, posing significant risks to privacy and security. To ensure real-world applicability, deepfake detectors must generalise effectively to unseen generators. However, most existing approaches rely on supervised training with both real and fake images, which limits their generalisation especially across generators categories (e.g. GANs vs DMs). In this work, we introduce $μ$Flow, a one-class deepfake detector trained only on real images without relying on pseudo-deepfakes or synthetic artifacts. Our approach builds on the observation that averaging multiple images amplifies consistent generative traces, producing highly discriminative feature representations. We leverage this property by modelling the distribution of features extracted from averaged images and training a normalizing flow to align the feature space of individual images with this distribution. This alignment yields a likelihood-based criterion that separates real and fake samples while promoting strong generalisation. We evaluate $μ$Flow on a fully out-of-distribution setting, where both real and fake datasets are unseen during training. Experimental results show that our method significantly outperforms SOTA detectors. Project page: https://opontorno.github.io/MuFlow.

---

## 论文详细总结（自动生成）

好的，请查收基于论文《μFlow: Leveraging Average Images for Improving Generalisation of Deepfake Faces Detectors》的结构化总结。

### **1. 核心问题与整体含义**

*   **研究背景与动机**:
    *   当前基于GAN和扩散模型的深度伪造技术已达到极高的逼真度，对隐私和安全构成严重威胁。
    *   现有的深度伪造检测器大多采用有监督训练，需要同时学习真实和伪造图像。这种做法导致模型容易过拟合于训练时见过的特定生成器留下的痕迹（如GAN的纹理），从而在面对**未见过的生成器**（尤其是跨类别，如从GAN泛化到扩散模型）时，泛化性能急剧下降。
    *   一些方法尝试通过仅用真实图像进行单类训练来提升泛化性，但或依赖生成伪伪造品（其质量成为瓶颈），或判别力不足。

*   **论文核心目标**:
    *   提出一种**不依赖任何伪造样本或合成伪影**的单类深度伪造检测器，从根本上避免对特定生成器痕迹的过拟合。
    *   通过创新的“平均图像”策略，大幅提升单类检测器的判别力和对未见生成器的泛化能力。

### **2. 方法论**

论文提出了名为 **μFlow** 的检测框架，其核心思想是：**利用对多张图像求平均来放大生成模型的细微痕迹，然后将单张图像的特征向这个更具判别力的平均图像特征空间对齐**。

*   **核心观察——平均图像的判别力**:
    *   如图2所示，单张真假图像的特征在t-SNE空间中混杂，难以区分（低类间变异性）。
    *   然而，对多张图像（如同为真实或同为伪造）进行平均后，其提取的特征在特征空间中会形成清晰可分的簇（高类间变异性）。这是因为平均操作强化了一致的生成痕迹，同时抑制了图像特有的噪声。

*   **关键技术流程**:
    1.  **构建判别性目标分布**:
        *   从训练集真实图像中随机采样K张图像进行平均，生成一组“平均真实图像”。
        *   使用预训练的编码器（如ResNet50）提取这些平均图像的特征。
        *   对这些特征拟合一个高斯混合模型，得到一个表征平均真实图像特征分布的概率密度函数 `P*_Z = N(μ\*, Σ\*)`。这个分布被认为是高度判别性的。

    2.  **训练归一化流用于特征对齐**:
        *   采用FastFlow作为基础架构，但修改了其目标分布。传统FastFlow将特征映射到标准正态分布 `N(0, I)`，而μFlow则训练一个归一化流 `fθ`，将**单张真实图像**的特征 `xR` 映射到上一步学到的**判别性分布 `P\*_Z`** 中。
        *   训练目标是最小化负对数似然，论文通过数学推导证明，这等效于最小化真实图像在判别性分布中的马氏距离，从而迫使单张真实图像的特征在映射后聚拢于该分布的高概率区域。

    3.  **推理与决策**:
        *   对于一张测试图像，提取其特征并通过训练好的归一化流映射至潜在空间。
        *   计算一个“假度分数”，该分数基于其在目标分布 `P\*_Z` 中的马氏距离和雅可比行列式。分数越高，表示该图像越远离真实图像的平均特征分布，即越可能是伪造的。
        *   根据训练集上计算的阈值 `τ` 进行二分类。

### **3. 实验设计**

*   **数据集与场景**:
    *   **训练集**: 仅使用 **FFHQ** 数据集中的真实人脸图像。
    *   **测试集 (完全分布外)**:
        *   **未见过的真实图像**: 来自 **CelebA-HQ** 数据集。
        *   **未见过的伪造图像**: 来自 **WILD** 数据集，并额外补充了3个GAN。这些生成器被分为三类：
            *   **GANs (生成对抗网络)**: StyleGAN系列、StarGAN等。
            *   **DM-OS (开源扩散模型)**: Stable Diffusion系列、Flux.1等。
            *   **DM-CS (闭源扩散模型)**: DALL-E 3, Midjourney等。

*   **基准对比方法**:
    *   **有监督的SOTA方法**: DFX-SN, FreqNet, NPR, ODDN, D³。这些方法在不同配置下训练，例如“Real + GANs”、“Real + DM-OS”、“Real + 混和生成器”，用于测试其跨类别泛化能力。
    *   **仅使用真实图像训练的方法**: OC-FakeDect, AFSD, PCL+I2G, SBI, AUNet, LAA-Net。

*   **评估指标**: 准确率， AUC, 平均精度。

### **4. 资源与算力**

*   **硬件配置**: 模型训练在**单张NVIDIA RTX A6000 GPU**上完成。
*   **训练时长**: 归一化流网络使用批量大小为32训练了**1000个epoch**。编码器（ResNet50）在训练期间被冻结，不参与更新。

### **5. 实验数量与充分性**

论文进行了大量且全面的实验来验证其有效性，评估较为充分和客观：

*   **主要结果对比**: 在1个训练集、1个真实测试集、3类（共19个）未见过的生成器集合上，与多达12种基线方法进行了系统性比较，覆盖了有监督和单类训练两种范式。
*   **鲁棒性分析**: 测试了模型在面对内容保留变换（如缩放、水平翻转、高斯噪声、椒盐噪声）以及信号退化扰动（如不同压缩率的JPEG压缩、不同标准差的高斯模糊）下的性能稳定性。
*   **消融实验**:
    *   分析了不同目标分布（标准高斯 vs. 从平均图像学得的判别性分布）对性能的决定性影响。
    *   对比了不同阈值决策方法（固定阈值、LOF、OC-SVM）的表现。
    *   探索了不同显著性水平 `α` 的影响。
*   **参数分析**: 通过轮廓分数分析了用于生成平均图像的采样数量K的最优值，找到了判别力与计算开销的平衡点。
*   **公平性**: 对于所有重现的基线方法，均使用其官方代码实现进行训练和测试，确保了对比的公平性。

### **6. 主要结论与发现**

*   **性能显著领先**: μFlow在完全分布外的严苛设置下，平均准确率达到**94.7%**，比表现最好的单类方法（LAA-Net, 83.0%）高出**+11.7%**。
*   **跨类别泛化能力极强**: 当同类别的有监督方法（如在GAN上训练）在跨类别测试（如在DM上测试）上表现不佳时，μFlow在所有三类伪造品（GANs, DM-OS, DM-CS）上均保持了超过90%的准确率，展示了对未知生成模型架构的卓越泛化能力。
*   **核心设计有效**: 将目标分布从标准正态分布改为从平均图像学得的判别性分布是性能巨大提升的关键，证明了“平均图像”策略的有效性。
*   **鲁棒性良好**: 模型对常见的图像扰动具有一定的鲁棒性，尤其是在面对信号级退化（如JPEG压缩）时性能表现稳定。

### **7. 优点**

*   **创新的训练范式**: 完全摒弃了对伪造样本和伪影的依赖，从根源上解决了模型过拟合于特定生成器痕迹的问题，是提升泛化性的理想路径。
*   **巧妙的思想**: 利用“平均图像放大痕迹”这一简洁而深刻的观察，解决了单类检测中真实与伪造特征区分度低的根本难题。
*   **理论与实践结合**: 提供了严谨的数学推导，揭示了最大化似然与最小化判别性空间马氏距离之间的等价关系，为方法提供了坚实的理论基础。
*   **强大的泛化性能**: 在极其困难的完全分布外场景下，性能大幅超越所有对比方法，证明了其对未知生成模型的有效性。

### **8. 不足与局限**

*   **仅限于人脸检测**: 目前的工作被限制在人脸这一单一语义类别，未验证其在其他物体或场景（如自然图像、卫星图等）上的深度伪造检测能力。
*   **未涉及视频**: 所有实验均在静态图像上进行，未扩展到更具挑战性的视频深度伪造检测任务，无法处理时序伪造线索。
*   **计算开销**: 生成平均图像集虽然只需在训练前计算一次，但当采样数量K值较大（如500或700）时，会产生不容忽视的计算和存储开销。
*   **潜在偏差**: 分布外测试是基于特定的数据集划分（训练集为FFHQ，真实测试集为CelebA-HQ），虽然证明了跨域能力，但可能仍隐含了特定数据集（如干净、高分辨率的人脸）的偏差。

（完）
