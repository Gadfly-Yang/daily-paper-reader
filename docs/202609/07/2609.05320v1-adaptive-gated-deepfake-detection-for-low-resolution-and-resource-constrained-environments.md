---
title: Adaptive Gated Deepfake Detection for Low-Resolution and Resource-Constrained Environments
title_zh: 面向低分辨率与资源受限环境的自适应门控深度伪造检测
authors: "Vaishnavi Sen, Cody Laurie, Rashida Hasan"
date: 2026-09-04
pdf: "https://arxiv.org/pdf/2609.05320v1"
tags: ["query:deepfake-fsl"]
score: 9.0
evidence: 针对低分辨率和资源受限环境的自适应门控深度伪造检测
tldr: 深度伪造检测模型通常依赖高分辨率输入和高算力架构，难以用于低资源与低分辨率环境。本文提出自适应门控框架AdaGate-DF，利用图像质量线索通过双多出口系统路由样本，使高质量图像可提前退出以节约计算。在Celeb-DF数据集上AUC达到0.9370，分辨率384×384时提升至0.9708，同时保持低推理延迟。在类别不平衡的FaceForensics++上也表现稳健，实现了检测性能与计算效率的实用平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.05320v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1804, \"height\": 653, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.05320v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1888, \"height\": 898, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.05320v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 611, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05320v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1867, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05320v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 917, \"height\": 656, \"label\": \"Table\"}]"
motivation: 深度伪造检测模型依赖高分辨率输入与高算力架构，难以部署于低资源和低分辨率场景。
method: 提出AdaGate-DF，利用图像质量线索通过双多出口系统实现自适应路由和提前退出，以节省计算。
result: 在Celeb-DF上AUC达0.9370，384×384时升至0.9708，优于MaD-CoRN和DefakeHop++，且延迟低。
conclusion: AdaGate-DF在类别不平衡数据下仍保持竞争性能，实现检测精度、不确定性感知与计算效率的平衡。
---

## 摘要
深度伪造检测模型通常依赖高质量输入、固定推理路径和计算开销高的架构，限制了它们在低分辨率和资源受限场景中的应用。本文提出 AdaGate-DF，一种自适应门控深度伪造检测框架，利用图像质量线索将样本路由至双多出口系统，使高质量图像能够提前退出并节省计算。我们在两个基准数据集（Celeb-DF 和 FaceForensics++）上，在多种配置下将 AdaGate-DF 与 MaD-CoRN、DefakeHop++ 和 ShuffleNetV2 进行评估，以测试图像分辨率依赖性以及训练和推理效率。在 Celeb-DF 上，AdaGate-DF 的 AUC 达到 0.9370，优于 MaD-CoRN 和 DefakeHop++，同时保持较低的推理延迟。基于分辨率的测试表明，随着输入分辨率提高，性能持续提升，在 384×384 分辨率下 AUC 达到 0.9708。FaceForensics++ 结果表明，AdaGate-DF 在类别不平衡情况下仍保持有效性，与所评估模型取得了具有竞争力的结果。总体而言，AdaGate-DF 在可变质量深度伪造检测中展示了检测性能、不确定性感知预测与计算效率之间的实用平衡。

## Abstract
Deepfake detection models often rely on high-quality inputs, fixed inference paths, and computationally expensive architectures, limiting their use in low-resolution and resource-constrained settings. This paper proposes AdaGate-DF, an adaptive gated deepfake detection framework that uses image-quality cues to route samples through a dual multi-exit system so high-quality images can exit earlier and save compute. We evaluated AdaGate-DF against MaD-CoRN, DefakeHop++, and ShuffleNetV2 on two benchmark datasets (Celeb-DF and FaceForensics++) under multiple configurations to test image resolution dependence and training and inference efficiency. On Celeb-DF, AdaGate-DF achieves an AUC of 0.9370, outperforming MaD-CoRN and DefakeHop++ while maintaining a low inference latency. Resolution-based testing shows consistent improvement as input resolution increases, reaching an AUC of 0.9708 at 384 by 384. The FaceForensics++ results highlight that AdaGate-DF remains effective under class imbalance, following competitive results with evaluated models. Overall, AdaGate-DF demonstrated a practical balance between detection performance, uncertainty-aware prediction, and computational efficiency for variable-quality deepfake detection.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：深度伪造检测模型通常依赖高质量输入、固定推理路径和高算力架构，难以在低分辨率、压缩、模糊等真实场景和资源受限环境中高效部署。
- **研究背景**：
  - 真实社交媒体中的图像/视频帧经常被压缩、缩放、模糊或分辨率不一致，削弱细粒度伪造痕迹。
  - 已有工作分别关注低分辨率鲁棒性、频域特征、轻量化架构或自适应推理，但往往只解决单一问题，且多数模型对所有样本使用同一推理路径，缺乏按输入质量分配算力的机制。
- **整体含义**：本文提出一种质量感知的自适应门控框架，希望在高检测性能与低计算成本之间取得实用平衡，更适合可变质量、低资源场景。

### 2. 论文提出的方法论
- **总体思想**：**AdaGate-DF** 采用**双分支 + 多出口门控**架构，结合 RGB 空间特征与 DCT 频域特征，根据图像质量特征动态选择“快速 / 中等 / 完整”三条推理路径。
- **关键模块**：
  - **双分支编码器**：
    - RGB 分支处理原始图像；
    - 频域分支对灰度图做 DCT 后提取频率特征，捕捉压缩和伪造伪影。
    - 每个分支有三个出口（浅层、中层、深层）。
  - **图像质量特征提取**：从每张图提取分辨率、模糊度、锐度、压缩代理、亮度、对比度等低层质量特征，构成向量 \( q \)。
  - **门控网络 \( G(\cdot) \)**：将 \( q \) 映射为路由决策 \( \hat{g} = G(q) \)，选择快速、中等或完整路径。
  - **路径定义**：
    - **Fast path**：使用 RGB 和频域分支浅层特征，计算量最小；
    - **Medium path**：使用中间层特征；
    - **Full path**：使用最深特征，融合后输出最终预测。
- **训练损失与算法流程**：
  - 联合训练所有出口和门控网络；
  - 总损失 \[ L_{\text{total}} = L_{\text{rgb}} + L_{\text{dct}} + L_{\text{fused}} + \lambda L_{\text{gate}} \]，其中 \( \lambda \) 控制门控损失权重；
  - 训练时随机采样分辨率 \( r \in \{128,224,256,384\} \)，增强分辨率鲁棒性；
  - 推理时先用门控网络得到路由决策，再只执行对应路径，节省算力。

### 3. 实验设计
- **数据集与场景**：
  - **Celeb-DF**：约 80,824 训练 / 10,104 验证 / 10,103 测试图像，类别近似平衡；
  - **FaceForensics++（FF++）**：从视频抽帧，按 2 FPS、每视频最多 8 帧，总 55,953 帧；训练/验证/测试按视频级 80/10/10 划分，真实与伪造比例约为 1:6，用于评估类别不平衡下的表现。
  - 输入分辨率：128×128、224×224、256×256、384×384，其中 224×224 作为基础推理分辨率。
- **对比方法（benchmark）**：
  - **MaD-CoRN**：轻量卷积储备网络方法；
  - **DefakeHop++**：基于手工特征提取的轻量检测器；
  - **ShuffleNetV2**：高效 CNN 架构；
  - 自身消融配置：完整门控模型、仅快速出口、仅中等出口、仅完整出口。
- **评估指标**：准确率、AUC、精确率、召回率、F1、不确定性率（低置信预测占比）、训练时间、推理延迟。
- **不确定性评估协议**：
  - AdaGate-DF：最大预测置信度低于 0.65 视为不确定；
  - 其他概率型模型：预测概率落在 [0.4, 0.6] 视为不确定；
  - 不确定性率 = 不确定样本数 / 总样本数 × 100%。

### 4. 资源与算力
- **硬件环境**：使用 CSU TIDE 高性能计算基础设施，GPU 加速节点配备 **NVIDIA L40 GPU，单卡 48 GB 显存**。
- **显式训练时长**：
  - AdaGate-DF 在 Celeb-DF 上训练约 **6.07 小时**；
  - 在 FF++ 上训练约 **5.58 小时**；
  - 论文图 2 也给出其他模型的训练时间和推理时间，但正文未列出每个模型的精确数值。
- **未明确说明**：
  - 未报告使用的 GPU 卡数（是否多卡并行）；
  - 未报告总 GPU-hours 或训练能耗；
  - 未说明所有模型是否在完全相同硬件协议下测量时序；作者在“未来工作”中提到“将在统一硬件协议下评估计时”。

### 5. 实验数量与充分性
- **实验组数大致统计**：
  - 2 个数据集 × 4 种分辨率 × 多种模型配置，形成多组分辨率鲁棒性实验；
  - 1 组主结果对比（Celeb-DF 和 FF++ 各一张表）包含 7 个模型/配置；
  - 1 组消融实验（完整门控 / 仅快速 / 仅中等 / 仅完整）；
  - 1 组训练与推理效率对比（图 2）；
  - 因此总体实验类型较丰富，覆盖精度、鲁棒性、不确定性、效率。
- **客观性与公平性评价**：
  - 优点：对比模型涵盖轻量、频域、手工特征等不同路线；所有模型在相同硬件上评估；分辨率评测较全面；不确定性评估被引入以弥补高置信误分类问题。
  - 不足：未报告多次随机运行或标准差，仅固定随机种子 42 的单次实验可能受随机性影响；未进行统计显著性检验；对比模型仅 3 个，缺少如 Xception、EfficientNet 等更主流检测器；FF++ 上所有模型 AUC 都很低，作者也承认需用平衡准确率、特异性、FPR/FNR 和混淆矩阵补充分析，但目前未给出这些指标。

### 6. 论文的主要结论与发现
- **Celeb-DF 主结果**：
  - AdaGate-DF 完整门控模型 AUC 达 **0.9370**，F1 为 **0.8654**，优于 MaD-CoRN 和 DefakeHop++，但低于 ShuffleNetV2（AUC 0.9993）；
  - 门控模型不确定性率为 22.23%，相比快速出口的 83.31% 和 MaD-CoRN 的 85.01% 大幅下降；
  - 完整出口模型性能最高（AUC 0.9709），但门控模型在节省算力与保持性能之间提供更优部署折中。
- **分辨率鲁棒性**：
  - AdaGate-DF 在 Celeb-DF 上随分辨率提高持续提升，384×384 时 AUC 达 **0.9708**；
  - ShuffleNetV2 在 224/256 分辨率上 AUC 最高，但在 384 分辨率下反而下降；
  - 在 FF++ 上 AdaGate-DF 比 DefakeHop++ 表现更稳定，AUC 波动范围较小（0.5043–0.5253 vs 0.2331–0.4883）。
- **效率结论**：
  - AdaGate-DF 在 Celeb-DF 推理延迟为 **0.14 ms/图**，FF++ 为 **0.25 ms/图**，在对比模型中较低；
  - DefakeHop++ 训练时间较低但推理延迟最高；MaD-CoRN 训练时间最高且推理延迟高于 AdaGate-DF；
  - ShuffleNetV2 训练时间低，但 AdaGate-DF 推理延迟更低且具备自适应路由和不确定性感知。

### 7. 优点
- **方法设计亮点**：
  - 将 **RGB 空间特征与 DCT 频域特征** 结合，提升对压缩/伪造伪影的表示能力；
  - **多出口门控机制** 根据图像质量动态分配计算，兼顾性能与效率；
  - 引入 **质量感知路由**，比单纯基于预测置信度的早期退出更有任务针对性；
  - 训练阶段随机多分辨率采样，增强模型对低质量输入的鲁棒性。
- **实验设计亮点**：
  - 同时评估精度、AUC、F1、不确定性率、训练时间和推理延迟，视角全面；
  - 消融实验区分快速、中等、完整出口，验证门控机制贡献；
  - 在 FF++ 上关注类别不平衡并用 AUC 作为主指标，避免阈值指标虚高；
  - 代码开源，可复现性较好。

### 8. 不足与局限
- **检测性能局限**：
  - 在 Celeb-DF 上弱于 ShuffleNetV2；
  - 在 FF++ 上所有模型 AUC 都很低（约 0.51–0.57），说明在强类别不平衡的真实伪造检测任务中，模型可能偏置到多数类（假样本），分类分离能力有限。
- **类别不平衡处理不足**：
  - 没有使用类别加权、过采样/欠采样或平衡损失等方法；
  - 未报告平衡准确率、特异性、FPR、FNR、混淆矩阵等，导致对类别不平衡下的性能判断不够全面。
- **统计稳健性不足**：
  - 固定随机种子、单次训练，缺少多次重复实验的标准差；
  - 未做统计显著性检验，难以判断改进是否显著。
- **门控机制未深入分析**：
  - 未报告不同分辨率下样本被分配到快速 / 中等 / 完整路径的比例；
  - 门控路由目标 \( g^* \) 的生成规则细节较简略；
  - 未讨论门控网络自身对低质量真实图像可能产生误路由的风险。
- **应用与泛化局限**：
  - 仅评估两个数据集，未见跨数据集泛化实验；
  - 对视频级时序建模没有涉及；
  - 未讨论对抗样本鲁棒性；
  - 推理时间极低（0.14 ms/图）在双分支结构下是否与实现细节、批处理或硬件测量方式一致，文中未给出足够说明，可能影响效率结论的可靠性；
  - 未来工作才计划使用统一硬件协议重新评估计时，表明当前效率比较可能存在协议不一致隐患。

（完）
