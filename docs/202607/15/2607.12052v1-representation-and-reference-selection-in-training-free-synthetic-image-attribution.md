---
title: Representation and Reference Selection in Training-Free Synthetic Image Attribution
title_zh: 免训练合成图像归属中的表征与参考选择
authors: "Meiling Li, Pietro Bongini, Benedetta Tondi, Mauro Barni"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12052v1"
tags: ["query:deepfake-fsl"]
score: 9.0
evidence: 采用免训练参考方法的合成图像归因
tldr: 训练无关的合成图像归属通过特征比对源特定参考来识别生成器，无需重新训练，但现有方法忽略了表示空间层次与参考构建策略的相互作用。本文对CLIP与DINOv2的不同层特征进行分析，并对比任意选择、语义对齐和再合成三种参考构建方式在不同参考预算下的表现。结果表明，归因准确率在模型中间层达到顶峰，因为深度语义抽象会掩盖辨别性细微线索；语义约束的参考能减少查询与参考间的不匹配，提升归因，尤其在参考有限时；再合成在少量参考下最有效，但成本高，而语义对齐在中等预算下兼顾精度和效率。该研究阐明了表示层次、参考选择与参考数量三者的关联，为无训练归因方法的设计提供了系统指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12052v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1491, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12052v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1870, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12052v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1895, \"height\": 684, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12052v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 925, \"height\": 446, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12052v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1233, \"height\": 421, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12052v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 927, \"height\": 130, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12052v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 918, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12052v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 157, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12052v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 916, \"height\": 196, \"label\": \"Table\"}]"
motivation: 无训练参考归因方法中，表示层次与参考构建策略的相互作用尚不清晰，限制了性能优化。
method: 在CLIP和DINOv2上提取多层特征，并对比任意、语义对齐及再合成参考选择方法，分析归因表现。
result: 中间层表示归因最强；语义约束参考有效缓解查询-参考差异，提升有限预算下鲁棒性；再合成低参考最佳，但语义对齐在中等规模下平衡精度与效率。
conclusion: 无训练归因应综合考虑表示层次、参考构建与数量；中层特征搭配语义对齐是实践中的高效策略。
---

## 摘要
合成图像归属旨在识别生成给定AI生成图像的生成器。免训练的基于参考的归属方法易于扩展，因为新出现的生成器可以通过添加源特定的参考而不是重新训练任务特定的分类器来纳入。其性能取决于两个耦合因素：用于比较的表征空间以及源特定参考的构建方式。然而，这两个因素之间的相互作用在很大程度上尚未被探索。在本文中，我们使用参考和现成的预训练表征对这种相互作用进行了受控分析。我们研究了从CLIP和DINOv2的不同层提取的表征，以及三种具有不同语义约束的参考选择方法：任意参考、语义对齐参考和基于再合成的参考。我们的结果表明，归属准确率在中间表征层次始终达到峰值，表明源区分线索在强语义抽象占主导地位之前更易获得。我们进一步表明，中间表征并非完全语义中立，这使得参考选择至关重要：语义约束的参考减少了查询与参考之间的不匹配，并改善了归属，特别是在参考预算有限的情况下。再合成在低参考情境中最有用，而当有中等大小的参考池可用时，语义对齐参考提供了更好的准确率-成本权衡。我们的发现表明，免训练的基于参考的归属应被理解为图像在何处进行比较、参考集如何构建以及有多少参考可用这三者之间的相互作用。

## Abstract
Synthetic image attribution aims at identifying the generator responsible for a given AI-generated image. Training-free reference-based attribution methods are easily scalable, since newly emerging generators can be incorporated by adding source-specific references rather than retraining a task-specific classifier. Their performance depends on two coupled factors: the representation space used for comparison and the way source-specific references are constructed. However, the interaction between these two factors remains largely unexplored. In this paper, we provide a controlled analysis of this interaction using references and off-the-shelf pretrained representations. We study representations extracted from different layers of CLIP and DINOv2, along with three reference selection methods with varying semantic constraints: arbitrary, semantically aligned, and resynthesis-based references. Our results show that attribution accuracy consistently peaks at intermediate representation levels, indicating that source-discriminative cues are more accessible before strong semantic abstraction dominates. We further show that intermediate representations are not completely semantically neutral, making reference selection critical: semantically constrained references reduce query-reference mismatch and improve attribution, especially under limited reference budgets. Resynthesis is most useful in low-reference regimes, while semantically aligned references provide a better accuracy-cost trade-off when a moderate-sized reference pool is available. Our findings show that training-free reference-based attribution should be understood as the interaction between where images are compared, how the reference set is constructed, and how many references are available.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：合成图像归属（Synthetic Image Attribution, SIA）旨在识别生成某张AI图像的具体生成器。随着生成模型不断涌现，传统的监督式指纹学习方法（需针对固定生成器集训练分类器）难以扩展。基于参考的免训练方法成为替代方案：通过将查询图像与各个候选源的参考图像进行比对来完成归属，而无需重新训练模型。
- **核心问题**：现有免训练参考归属方法的性能取决于两个紧密耦合的因素——**表征空间**（在哪个特征空间进行比较）和**参考选择策略**（如何为每个候选源构建参考集）。然而，这两个因素之间的相互作用机制尚不清晰，已有工作多将其作为整体方法的组成部分，缺乏对两者单独贡献及其兼容性的受控分析。
- **整体含义**：本文旨在系统解耦表征空间与参考选择，揭示不同语义约束条件下参考构建方式与不同抽象层次特征之间的关系，为免训练归属系统的设计提供基础性理解。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

#### 2.1 核心思想
将免训练参考归属形式化为一个**查询-参考匹配问题**，并明确分解为：参考选择方法（π）、表征空间（ρ）、参考数量（M）和评分聚合规则（α）等可控轴，通过控制实验分析这些因素对归属准确率的交叉影响。

#### 2.2 统一框架

- **候选源**：设 \( \mathcal{G} = \{G_1, \dots, G_K\} \) 为生成器集合。
- **参考集**：对于查询图像 \( x_q \)，在参考选择方法 \( \pi \) 下，每个候选源 \( G \) 获得参考集 \( \mathcal{R}^\pi_G(x_q; M) \)。若参考选择与查询无关，则为 \( \mathcal{R}^\pi_G(M) \)。
- **表征空间**： \( \rho = (b, l) \)，其中 \( b \) 为冻结的视觉骨干网络（CLIP 或 DINOv2 的 ViT-L/14），\( l \) 为某一层的索引。特征提取为 \( \phi_\rho(x) = \phi_{b,l}(x) \)，使用 CLS token。
- **相似度得分**：查询与某个参考 \( r \) 的成对相似度定义为 \( a_\rho(x_q, r) = \text{sim}(\phi_\rho(x_q), \phi_\rho(r)) \)（通常为余弦相似度）。
- **源级得分**：将同一源内的所有参考相似度通过聚合规则 \( \alpha \)（如最大值、均值、softmax加权均值、kNN投票）汇聚为源得分 \( s_{\rho,\pi,M,\alpha}(x_q, G) \)。
- **归属决策**：\( \hat{G} = \arg\max_{G \in \mathcal{G}} s_{\rho,\pi,M,\alpha}(x_q, G) \)。

#### 2.3 参考选择方法（π）

三种方法构成语义约束强度递减的层次：
1. **再合成参考（Resynthesis）**：黑盒方式，用查询图像的文本描述（由LLaVA-v1.5-7B生成）查询每个候选生成器，生成与查询语义对齐的参考。语义对齐最强。
2. **语义对齐参考（Semantically aligned）**：仅使用预收集的源样本池，从中选取与查询图像具有相同语义类别（如面部、动物等）的图像作为参考。语义对齐中等。
3. **任意参考（Arbitrary）**：从样本池中随机采样参考，不考虑查询内容。语义约束最弱。

#### 2.4 表征空间探查
使用冻结的 CLIP-ViT-L/14@336 和 DINOv2-ViT-L/14，从不同 transformer 层提取特征，观察归属准确率随层级深度的变化趋势。

### 3. 实验设计：数据集、基准及对比方法

#### 3.1 数据集
- **FaceResyn**：外部人脸基准，包含10个生成器的人像查询与对应再合成参考（每查询每源1个参考），用于约束较强的人脸场景。
- **BC-Attr-6**：新构建的“偏见受控”数据集，共10个生成器，涵盖6个语义类别（面部、动物、建筑、全景、卫星视图、车辆）。每个类别-生成器对包含200张图像（20张查询，180张参考），总计12,000张图像。类别标签支持语义对齐参考与任意参考的对比。
- **COCO-Attr**：使用MSCOCO字幕作为提示生成的多样化数据集，同样10个生成器，每生成器100张查询图像，无预定义语义类别，用于评估开放内容分布下的表现。

#### 3.2 基准与指标
- 任务：闭集源归属（查询属于已知候选生成器之一）。
- 指标：**Top-1准确率（ACC）**。
- 默认配置：若无特别说明，每源参考数 \( M=10 \)，评分聚合规则为最大余弦相似度。

#### 3.3 对比维度
- **表征层**：CLIP 和 DINOv2 的 24 个 transformer 层（含浅层、中层、深层）。
- **参考选择方法**：再合成、语义对齐、任意。
- **参考预算 \( M \)**：从 1 到 100 不等。
- **评分聚合规则**：最大相似度、平均相似度、softmax加权平均、top-5 kNN投票。
- **鲁棒性测试**：JPEG 压缩（Q=70-100）、缩放（r=0.35-1.0）、高斯模糊（σ=1-3）。
- **诊断实验**：在冻结特征上训练线性分类器，测量各层的语义可分性与生成器可分性。

### 4. 资源与算力
- 论文中**未明确提及**所使用的 GPU 型号、数量、训练时长或具体算力开销。原因在于方法完全免训练：所有特征提取均基于冻结的预训练模型（CLIP、DINOv2），归属过程仅涉及前向传播与相似度计算；再合成参考通过调用生成器 API 获得，其生成成本不属于模型训练范畴。虽然文中未报告推理硬件，但可合理推断实验在常见 GPU（如单块 A100 或 V100）上即可完成。

### 5. 实验数量与充分性
- 实验设计极为系统，可视为多个因子在3个数据集上的交叉验证，包括：
  - **不同表示空间**：2个编码器 × 24层 = 48种表征，跨不同参考方法和预算绘图。
  - **参考选择方法**：3种方法 × 多档 \( M \)（1、2、4、8、10、25、50、100）的全面对比。
  - **消融实验**：4种评分聚合规则、3类后处理扰动各4个强度等级。
  - **诊断性实验**：语义与生成器线性可分性分析。
- 总体实验组数难以精确统计，但估计超过 50 组大型交叉实验，充分覆盖了所提框架的可控维度。
- 实验设计**客观公平**：相同基准数据集下直接对比不同表示与参考选择，消除了无关变量；多个数据集和编码器的使用增强了结论的普适性。

### 6. 论文的主要结论与发现
- **中间层表征最优**：无论是 CLIP 还是 DINOv2，归因准确率总是在中间层（如L12左右）达到峰值。深层语义抽象会掩盖源区分线索，浅层则包含过多低层变异。
- **中间层仍具有语义偏向**：诊断实验表明，深层语义可分性强但生成器可分性下降，中间层虽更适合归属，但并非语义中立。因此，参考语义与查询的匹配至关重要。
- **语义约束参考至关重要**：在相同表征下，语义对齐或再合成参考始终大幅优于任意参考。减少查询-参考语义不匹配可有效提升归属性能。
- **参考预算决定最佳选择策略**：
  - 当参考极少（\( M=1, 2 \)）时，**再合成**效果最佳（如 L12 精度可达 60%–68%）。
  - 当有中等参考池（\( M \geq 4 \)）时，**语义对齐参考**反超再合成，并在 \( M=100 \) 时达到 95.75% 准确率，且无需昂贵的推理时生成代价。
  - 任意参考即使在大预算下效率也较低。
- **后处理鲁棒性**：JPEG、缩放、模糊会降低绝对准确率，但不改变中间层表现最优的基本趋势。

### 7. 优点：方法或实验设计上的亮点
- **因子化解构**：首次将免训练参考归属明确拆分为表征空间与参考选择两个独立轴，并系统分析其交互，为后续研究提供了清晰的分析框架。
- **实验设计严谨**：构建了两个新数据集（BC-Attr-6 和 COCO-Attr），可分别控制语义类别和模拟自由分布，同时引入既有基准 FaceResyn，三重验证增强结论可信度。
- **表征层级诊断**：通过线性探针展示了语义抽象与生成器判别性之间的trade-off，为“中间层最优”提供了机理性解释。
- **实用指导明确**：给出了不同参考预算下的策略选择建议——极低预算用再合成，中等预算用语义对齐，直接面向现实部署。
- **跨编码器验证**：同时使用 CLIP 和 DINOv2 两个结构相似但训练目标不同的视觉模型，证实趋势的通用性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **生成器范围有限**：实验仅涵盖 10 个主流文本到图像生成器，对于更多样化或基于GAN的生成器、不同架构的泛化性未验证。
- **闭集假设**：仅在已知候选源集合内进行闭集归属，未涉及开集识别（查询来自未知生成器）或拒识场景。
- **再合成依赖外部描述模型**：再合成参考的质量受限于图像描述模型（LLaVA），若描述不准或引入偏差，可能影响性能。且再合成的生成成本较高，不适合大规模实时应用。
- **预训练模型的选择**：仅分析了 ViT-L/14 架构的 CLIP 和 DINOv2，未涵盖其他规模（如ViT-B、ViT-H）或不同预训练范式（如SigLIP、MAE）的编码器，结论的广度尚待拓展。
- **语义对齐的标签依赖**：语义对齐参考要求图像具有语义标签，COCO-Attr 等完全非结构化数据集上难以直接应用，需要额外的自动标注或检索手段。
- **算力信息缺失**：未报告任何推理阶段的时间或内存开销，例如不同层特征提取速度、大规模参考库下的相似度搜索成本，对实际部署的参考价值有限。
- **可能存在的偏差**：数据集构建时固定了提示词和采样策略，可能引入特定生成器的风格偏好；仅使用CLS token作为图像表征，未探讨patch token聚合等替代方案。

（完）
