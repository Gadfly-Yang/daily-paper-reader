---
title: "DECODE: Tackling Representation and Decision Degradation in Continual AI-Generated Image Detection"
title_zh: DECODE：应对持续式AI生成图像检测中的表征与决策退化
authors: "Zihao Cai, Xinghan Li, Ruiyan Yang, Xue Song, Haijun Shan, Jingjing Chen"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.27882v1"
tags: ["query:deepfake-fsl"]
score: 9.0
evidence: 跨生成域的持续AI生成图像检测
tldr: "随着生成模型不断演进，AI生成图像检测需要在持续学习框架下适应新生成域，但检测器极易遭受灾难性遗忘，尤其是表示和决策层面的双退化。本文提出DECODE框架，通过子空间多样性正则化（SDR）维持取证特征的多样性，并设计闭式决策对齐（CDA）在每次适配器合并后无需调参即可重新对齐分类边界。在涵盖19个生成域的基准上，DECODE取得99.36%的平均准确率，遗忘率骤降至0.39%，还展现出对11个未知生成器95.36%的强大泛化能力。该工作揭示了持续检测中的双退化新机理，并提供了有效应对方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27882v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1836, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27882v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1819, \"height\": 745, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27882v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 411, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27882v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1816, \"height\": 532, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27882v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1828, \"height\": 643, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27882v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1826, \"height\": 756, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27882v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1702, \"height\": 461, \"label\": \"Table\"}]"
motivation: 现有方法仅关注表示稳定，忽视决策边界漂移，导致检测器在持续学习中性能下降，需同时解决表示与决策双重遗忘。
method: 提出解耦持续检测框架DECODE，集成子空间多样性正则化（SDR）保持取证特征多样性，闭式决策对齐（CDA）自动校准分类头。
result: "在19个生成域上达99.36%准确率，遗忘率仅0.39%，在11个未见生成器上泛化95.36%。"
conclusion: 揭示了持续AI图像检测的双退化问题，DECODE同时缓解表示与决策级遗忘，实现高准确率和低遗忘，并具备出色泛化能力。
---

## 摘要
随着生成模型的持续演进，AI生成图像检测器必须逐步适应新出现的生成领域，同时保留从先前领域获取的知识。这种持续学习的设置尤其具有挑战性，因为取证痕迹往往十分细微且具有生成器特异性，使得检测器极易遭受灾难性遗忘。现有方法主要通过稳定特征表示来解决该问题，隐含地将遗忘视为表征层面的问题。在本文中，我们指出该视角并不完整。我们证明，即使特征表示仍然具有判别力，随着分类头在新领域上持续优化，决策边界也会逐渐漂移。这两种效应共同导致了一种复合失效模式，称为双重退化。为克服这一挑战，我们提出了DECODE，一种解耦的持续检测框架，该框架同时缓解表征层面和决策层面的遗忘。具体而言，我们引入子空间多样性正则化（SDR）来保留多样化的取证表征，以及闭式决策对齐（CDA）以在每次适配器合并后重新校准共享分类头，而无需手动调整超参数。在19个生成域上的大量实验表明，DECODE的平均准确率达到99.36%，遗忘率仅为0.39%，同时在11个未见生成器上泛化准确率达到95.36%。

## Abstract
As generative models continue to evolve, AI-generated image detectors must incrementally adapt to emerging generative domains while preserving knowledge acquired from previous ones. This continual learning setting is particularly challenging because forensic traces are often subtle and generator-specific, making detectors highly vulnerable to catastrophic forgetting. Existing methods primarily address this problem by stabilizing feature representations, implicitly treating forgetting as a representation-level issue. In this paper, we show that this perspective is incomplete. We demonstrate that even when feature representations remain discriminative, the decision boundary can progressively drift as the classification head is continually optimized on new domains. These two effects jointly give rise to a compound failure mode, termed Dual Degradation. To overcome this challenge, we propose DECODE, a decoupled continual detection framework that jointly mitigates representation- and decision-level forgetting. Specifically, we introduce Subspace Diversity Regularization (SDR) to preserve diverse forensic representations and Closed-Form Decision Alignment (CDA) to recalibrate the shared classification head after each adapter merge without manual hyperparameter tuning. Extensive experiments on 19 generative domains show that DECODE achieves an average accuracy of 99.36% with only 0.39% forgetting, while further generalizing to 11 unseen generators with 95.36% accuracy.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **研究动机**：AI 生成图像检测器需要持续学习以适应不断涌现的新生成模型（如扩散模型、GAN、人脸伪造等），但灾难性遗忘严重削弱其对旧领域的检测能力。
- **现有局限**：现有方法普遍将遗忘归因于*特征表示退化*（representation degradation），忽视了另一个关键因素——*决策边界漂移*（decision boundary drift）。即使特征仍具有良好的可分性，持续训练的分类头也会使决策边界偏向新任务，导致对旧任务的准确率大幅下降。
- **核心问题**：首次明确指出持续检测中存在“双重退化”（Dual Degradation）——表示层与决策层同时退化，且二者需被分别应对。

## 2. 论文提出的方法论
- **整体框架**：提出 **DECODE**，一种解耦的持续检测框架，分别在两个阶段独立处理表示退化与决策退化。
- **阶段一：表示学习——子空间多样性正则化（SDR）**
  - 在冻结的视觉编码器（CLIP ViT-L/14）中插入低秩适配（LoRA），仅训练适配器和分类头。
  - **正交性损失（\(\mathcal{L}_{\text{orth}}\)）**：同时对*任务内*和*任务间*的 LoRA 秩方向施加正交约束，促进方向多样性与子空间分离，减少干扰。
    \[
    \mathcal{L}_{\text{orth}} = \sum_l \left( \| \mathbf{G}^A \odot \bar{I} \|_F^2 + \| \mathbf{G}^B \odot \bar{I} \|_F^2 \right)
    \]
    其中 \(\mathbf{G}^A, \mathbf{G}^B\) 为所有任务的 A/B 矩阵的 Gram 矩阵，\(\bar{I}\) 屏蔽对角线以惩罚非对角项。
  - **能量平衡损失（\(\mathcal{L}_{\text{bal}}\)）**：惩罚不同秩方向（rank）上因子矩阵行/列范数的方差，防止少数方向独大，充分利用所有秩容量。
    \[
    \mathcal{L}_{\text{bal}} = \sum_l \left[ \text{Var}_i\left(\|A^{(k)}_{l,i:}\|_2^2\right) + \text{Var}_i\left(\|B^{(k)}_{l,:i}\|_2^2\right) \right]
    \]
  - 总训练目标：\(\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{CE}} + \lambda_{\text{orth}}\mathcal{L}_{\text{orth}} + \lambda_{\text{bal}}\mathcal{L}_{\text{bal}}\)。
  - 任务训练完成后将 LoRA 参数合并至骨干网络，保持单一模型，推理无额外开销。
- **阶段二：决策对齐——闭式决策对齐（CDA）**
  - 维护一个紧凑的范例记忆库（每类随机保留 \(m\) 张图像），在每次适配器合并后，用更新后的骨干网络重新提取所有范例的特征。
  - 使用闭式岭回归（ridge regression）解析解直接计算新的分类头权重：
    \[
    \tilde{W}^* = (\tilde{X}^\top \tilde{X} + \lambda \tilde{I})^{-1} \tilde{X}^\top Y
    \]
    其中 \(\tilde{X}\) 为增广特征矩阵，\(\lambda\) 为正则化系数。
  - **自动选择 \(\lambda\)**：通过留一交叉验证（LOO-CV）在范例集上计算最优 \(\lambda\)，全程无需手动调参或验证集。

## 3. 实验设计
- **数据集与场景**：覆盖19个异构生成模型，来源包括 AIGIBench、CDDB、ARForensics、NeuroRenderedFake、T2I-CoReBench 等，包含扩散模型、GAN、人脸交换、3DGS、自回归生成器等。
- **评估协议**：
  - **Protocol 1（持续学习）**：8个任务顺序学习（SD3 → Imagen3 → StyleGAN3 → BlendFace → GauGAN → GSGen → Infinite‑ID → Infinity），混合多种合成范式以制造高度异构的遗忘压力。
  - **Protocol 2（开放世界泛化）**：在 Protocol 1 训练结束后，直接测试在11个完全未见的生成器上的准确率，无需任何额外适应。
- **对比方法**：
  - 非持续方法（顺序微调）：NPR、SAFE、Effort、IAPL、LTD。
  - 无回放的持续方法：DevFD、SAIDO。
  - 有回放的持续方法：DFIL、SUR‑LID、Tang et al.。
  - DECODE 的无回放变体（仅 SDR，冻结分类头）和完整版（SDR+CDA）分别与对应组别比较。
- **评价指标**：平均准确率（AA）、平均遗忘（AF）、新任务准确率（New ACC）。
- **额外测试**：对 JPEG 压缩、图像缩放、高斯模糊的鲁棒性评估。

## 4. 资源与算力
- 文中**未明确说明**所使用的 GPU 型号、数量以及具体的训练耗时。
- 仅提到训练配置：Adam 优化器，学习率 \(10^{-3}\)，批大小 16，每个任务训练 10 个 epoch；基础模型为 CLIP ViT‑L/14，输入分辨率 224×224。范例内存每类保留 50 张图像。

## 5. 实验数量与充分性
- 实验层面相当全面，至少包含：
  - 与 9 种先进方法的持续学习对比（Protocol 1），逐任务报告 AA 与 AF。
  - 11 个未见生成器的泛化能力对比（Protocol 2）。
  - 三种图像扰动下的鲁棒性曲线（JPEG、缩放、模糊）。
  - 详细的消融实验，逐步添加 \(L_{\text{orth}}\)、\(L_{\text{bal}}\)、CDA，并对比基于梯度下降的决策对齐。
  - 对 LoRA 秩能量分布和方向冗余的可视化分析。
- 实验设计公平：统一数据划分，所有方法使用官方实现，重复 3 次取均值。虽然主表仅展示一种任务顺序，但论文指出补充材料中提供了其他顺序的结果，进一步增强了结论的可靠性。

## 6. 论文的主要结论与发现
- 持续 AI 生成图像检测面临“双重退化”：仅稳定特征表示不足以遏止遗忘，决策边界漂移同样关键。
- DECODE 通过 **SDR** 维持取证特征的多样性，**CDA** 自动重新对齐决策边界，有效解耦并缓解两种退化。
- 在 19 个生成域上，DECODE 达到 **99.36% 的平均准确率**，遗忘率仅 **0.39%**；在无回放模式下仍保持 99.14% AA、0.70% AF，大幅优于现有最佳方法。
- 表现出极强的**开放世界泛化能力**：对 11 个未见生成器的平均准确率达 **95.36%**，且在每个生成器上均最优。
- 对 JPEG 压缩、缩放、高斯模糊等常见扰动具有显著更优的鲁棒性。

## 7. 优点
- **问题洞察深刻**：首次明确提出并验证了决策退化这一被忽视的遗忘因素，拓宽了持续检测的研究视角。
- **解耦设计精妙**：SDR 和 CDA 分别靶向表示与决策退化，互不干扰且可灵活组合；合并适配器保持单骨干网络，无推理开销增长。
- **闭式决策对齐**：CDA 使用岭回归解析解，配合 LOO‑CV 自动选参，免去额外梯度优化与超参数调节，简洁高效。
- **实验充分且成绩突出**：在 19 域持续学习、11 域泛化、鲁棒性测试中均大幅领先，遗忘率极低，强烈证明方法的有效性。

## 8. 不足与局限
- **仍依赖少量范例**：完整版 DECODE 需要每类存储 50 张映像，虽然量小，但在严格禁止存储旧数据的场景下受限；不过无回放变体已表现出较强竞争力。
- **计算开销未分析**：SDR 中正交损失需跨任务计算，可能增加训练时间；论文未给出训练速度或内存对比。
- **任务顺序与分辨率固定**：主实验采用特定任务顺序，输入 224×224；对更高分辨率及不同任务顺序的敏感性虽在补充材料中提及，但正文未展开。
- **模型与预训练依赖**：方法基于 CLIP ViT‑L/14，迁移到其他视觉主干时需重新验证；CLIP 自身可能已包含某些生成模型的先验知识。
- **未涉及对抗与社交平台场景**：未测试对抗性攻击、社交媒体后处理的复杂退化情况，离完全现实部署尚有距离。

（完）
