---
title: Probabilistic Robustness in Medical Image Classification
title_zh: 医学图像分类中的概率鲁棒性
authors: "Yi Zhang, Siddartha Khastgir, Xingyu Zhao"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03797v1"
tags: ["query:deepfake-fsl"]
score: 8.0
evidence: 研究深度学习模型在医学图像分类中的概率鲁棒性
tldr: 深度学习在医学图像分类中性能优异，但预测受扰动时易出错，现有研究聚焦于对抗鲁棒性，难以反映真实临床场景。本文提出以概率鲁棒性作为更实用的模型可信度衡量，通过构建自然损坏配置在MedMNIST v2上系统评估常见模型。结果从统计学角度揭示了模型在自然损坏下的表现差异，为医学影像模型的安全部署提供了新的评估依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03797v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1640, \"height\": 404, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03797v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1378, \"height\": 655, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03797v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1522, \"height\": 187, \"label\": \"Table\"}]"
motivation: 现有对抗鲁棒性评估设定脱离真实临床扰动，亟需更实际的可信度衡量方法。
method: 构建医学图像的自然损坏配置，并在MedMNIST v2数据集上系统评估常用深度学习模型的概率鲁棒性。
result: 评估揭示了模型在自然损坏下的性能差异，提供了统计可信度的新视角。
conclusion: 概率鲁棒性评估更符合医学应用需求，有助于模型在安全关键场景下的可信部署。
---

## 摘要
深度学习在医学图像分类中展现出强大性能，但在安全关键的临床环境中，其可信部署仍具挑战性，因为扰动下的预测错误可能导致严重后果。现有研究主要从最坏情况视角关注对抗鲁棒性，然而此类设置可能无法代表真实的医学应用。在本工作中，我们研究概率鲁棒性，作为模型可信度更实用的衡量指标。为此，我们构建了一系列医学图像分类的自然损坏设置，并在MedMNIST v2数据集上系统评估了常用的深度学习模型。我们的研究从统计角度为评估深度学习模型的可信度提供了基础，从而支持其在医学影像应用中更可信的部署。

## Abstract
Deep learning (DL) has shown strong performance in medical image classification, but its trustworthy deployment remains challenging in safety-critical clinical settings, where prediction errors under perturbations may lead to severe consequences. Existing studies mainly focus on adversarial robustness (AR) from a worst-case perspective; however, such settings may be less representative of real medical applications. In this work, we investigate probabilistic robustness (PR) as a more practical measure of model trustworthiness. To this end, we construct a set of natural corruption settings for medical image classification and systematically evaluate commonly used DL models on MedMNIST v2 dataset. Our study provides a statistically grounded perspective on assessing the trustworthiness of DL models, thereby supporting their more trustworthy deployment in medical imaging applications.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：深度学习在医学图像分类中虽然性能优异，但现有鲁棒性研究大多关注**对抗鲁棒性（AR）**，即从最坏情况出发评估模型是否能抵抗刻意构造的微小干扰。然而，这种设定与真实临床场景中常见的、非对抗性的自然图像损坏（如模糊、亮度变化）相去甚远，导致鲁棒性评估的实际参考价值有限。
- **整体含义**：作者提出用**概率鲁棒性（PR）** 作为更贴合真实应用场景的可信度量指标。PR 衡量的是在给定大小的随机扰动下模型预测仍保持正确的**概率**，而非是否存在一个可以导致模型错误的确定性扰动。该研究旨在建立一个从统计视角出发的评估框架，以更准确地反映模型在自然损坏条件下的可信程度，为医学影像深度学习模型的安全部署提供参考。

### 2. 方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：以概率鲁棒性代替传统的最坏情况鲁棒性，将局部鲁棒性评估从“是否存在对抗样本”转变为“对抗样本出现的可能性有多大”，从而提供一种整体的、统计化的模型鲁棒性评价。
- **概率鲁棒性的定义**：
  - 对输入 \( x \)，在半径为 \( \gamma \) 的 \( L_p \) 范数球内，PR 定义为模型在随机扰动下预测保持不变的概率：
    \[
    R(x, \gamma) = \mathbb{E}_{\delta \sim P_r(\cdot|x), \|\delta\| \le \gamma}\left[ \mathbf{I}_{\{f_\theta(x+\delta)=y\}}(x+\delta) \right]
    \]
  - 其中 \( \mathbf{I} \) 为指示函数，\( P_r(\cdot|x) \) 表示生成随机扰动的局部输入分布。从频率角度看，PR 可解释为无穷多次独立扰动中预测保持正确的相对频率，即非对抗样本所占的比例。
- **自然损坏配置构建**：将通用计算机视觉中常见的自然扰动类型适配到医学图像上，选取六种典型损坏：
  - **Defocus**（散焦模糊）
  - **Motion blur**（运动模糊）
  - **Stain**（污渍）
  - **Saturate**（饱和度变化）
  - **Pixelate**（像素化）
  - **Bright+/−**（亮度增减）
- **评估协议**：参照 PRBench 的基准测试方式，在自然损坏条件下计算各模型在测试集上的 PR 值，并与标准干净图像上的分类准确率（ACC）和 AUC 进行对比。

### 3. 实验设计
- **数据集**：MedMNIST v2 中的 **PathMNIST**，共 107,180 张 9 类结肠病理图像样本，划分为训练集 89,996、验证集 10,004、测试集 7,180。
- **模型**：两种常用 CNN 骨干网络——**ResNet‑18** 和 **ResNet‑50**，均按照 MedMNIST v2 协议训练至收敛。
- **评估指标**：
  - 干净图像：ACC 和 AUC。
  - 六种自然损坏条件下：概率鲁棒性（PR）。
- **对比对象**：研究并非以提出新方法为目的，而是评估现有模型；对比存在于不同模型之间，以及同一模型在干净与损坏条件下、不同损坏类型之间的表现差异。

### 4. 资源与算力
- 论文**未明确说明**所使用的 GPU 型号、数量、训练时长或推理时的硬件环境。
- 从实验规模看（PathMNIST 为中规模图像分类，ResNet‑18/‑50 为轻量级网络），训练所需算力较小，但具体细节在文中完全缺失。

### 5. 实验数量与充分性
- **实验组数**：
  - 2 种模型（ResNet‑18、ResNet‑50）
  - 1 个数据集（PathMNIST）
  - 6 种自然损坏类型（亮度变化包含两个方向，但合并为一个指标 ±）
  - 对应的干净图像基线
- 整体共计约 **2×7** 组主要评估结果，呈现在单一表格中。
- **充分性与客观性分析**：
  - 实验设计较为**简化**，仅在单一医学图像数据集和两种类似架构上进行了评估，未涉及更多样的模态（如 X 光、CT）或不同复杂度模型。
  - 未进行消融实验，也未与任何增强模型鲁棒性的方法（如对抗训练、数据增强）对比，不能完全反映改进潜力。
  - 所用的自然损坏类型虽来自通用视觉领域，但未深入论证其在医学影像中的实际出现频率与严重程度，可能引入一定选择性偏差。
  - 整体实验可作为初步框架验证，但作为系统研究，其广度和深度尚有不足。

### 6. 主要结论与发现
- **自然损坏显著削弱模型可信度**：尽管干净图像上 ResNet‑18/‑50 的 ACC 分别高达 90.68% 和 92.05%，在六种自然损坏下二者的 PR 均大幅下降，例如亮度变化下 PR 仅 62.84%~76.83%。
- **PR 高度依赖损坏类型**：两种模型对 Pixelate、Stain 和 Saturate 保持相对较高的鲁棒性，但对 **Motion blur** 极其脆弱（PR 低至约 40%），Defocus 和亮度变化也造成显著退化，表明模糊类干扰比颜色表观变化更具挑战。
- **更高干净准确率未必带来更好鲁棒性**：尽管 ResNet‑50 的 ACC/AUC 高于 ResNet‑18，但其 PR 并非在所有损坏类型上均更优（例如 Stain 和 Saturate 下 ResNet‑18 略高），说明不考虑扰动环境下的强性能并不直接等价于实际可信度。

### 7. 优点
- **概念新颖且实用**：将概率鲁棒性引入医学图像分类，弥补了传统对抗鲁棒性在真实场景下代表性不足的问题，强调“可能性”而非“存在性”。
- **可操作的自然损坏基准**：明确构建了一套适用于医学图像的自然损坏设定，为后续研究提供了可复现的评估框架。
- **清晰的统计视角**：从频率解释和期望值公式出发，使 PR 定义具有严格的数学基础，易于定量衡量和比较。
- **直接对比干净性能与鲁棒性**：通过并列 ACC/AUC 和不同损坏下的 PR，直观揭示“性能好不等于抗干扰强”，警示单纯追求干净指标的风险。

### 8. 不足与局限
- **实验覆盖有限**：仅在一个数据集、两种相似网络上进行测试，未扩展到更多成像模态（CT、MRI、X 光等）或更深的模型，结论的泛化性存疑。
- **评估对象单一**：未包含任何旨在提升鲁棒性的技术（如对抗训练、风格增强），未能展示 PR 在指导模型改进方面的实际效用。
- **自然损坏的真实性未充分论证**：所用损坏虽来自常见设定，但在医疗影像中的发生概率和程度缺乏临床调研或实证支持，可能不适合某些特定成像环境。
- **未讨论扰动半径与临床容错的关系**：扰动球半径 \( \gamma \) 的选取未见明确依据，未与医学诊断的允许误差范围关联，因此 PR 的绝对值在临床决策中的意义尚不明朗。
- **忽略分类类别的不平衡特性**：PR 是整体概率，未按类别分析，可能掩盖某些疾病类对扰动特别敏感的风险。
- **无算力与复现细节**：论文未提供训练超参、硬件配置或代码，对独立复现造成一定障碍。

（完）
