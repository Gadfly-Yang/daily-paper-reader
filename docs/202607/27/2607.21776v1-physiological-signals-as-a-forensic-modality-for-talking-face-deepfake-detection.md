---
title: Physiological Signals as a Forensic Modality for Talking-Face Deepfake Detection
title_zh: 生理信号作为说话人脸深度伪造检测的法医模态
authors: "Othmane Harraq, Tamer Aldwairi"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21776v1"
tags: ["query:deepfake-fsl"]
score: 9.0
evidence: 使用生理信号进行说话人脸深度伪造检测
tldr: "说话人脸深度伪造从单张静态图像与音频生成逼真视频，当前图像检测器普遍失效，因其合成过程无真实视频继承生理信号。本文认为远程光电容积描记(rPPG)对此类伪造有独特优势，提出利用RhythmFormer提取每视频rPPG波形，并训练一维ResNet等轻量分类器。在Celeb-DF++的说话人脸子集上，严格身份独立设置下，仅用生理通道的1D ResNet取得AUC 0.806、EER 27.8%，仅比最佳通用检测器Effort (ICML 2025) 低2.4点。同时，复现先前的rPPG检测器DeepFakesON-Phys发现，其在旧数据上AUC从0.999大幅降至0.622，凸显了该问题的挑战性。不同生成器间检测AUC跨度达0.985至0.690，且排序高度稳定，表明生理伪造难度是生成器的固有特性，这构成了本文的核心理论贡献。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21776v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 850, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21776v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 830, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21776v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 673, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21776v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 737, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21776v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 274, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21776v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 748, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21776v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 772, \"height\": 551, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21776v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 997, \"height\": 548, \"label\": \"Table\"}]"
motivation: 当前图像检测器无法识别说话人脸伪造，因其合成过程无真实视频继承生理特征，rPPG因此成为有效检测模态。
method: 提出基于RhythmFormer的检测框架，从视频提取rPPG波形，训练一维ResNet等轻量分类器区分真伪。
result: 在Celeb-DF++的TF子集上，1D ResNet仅凭生理信号取得AUC 0.806，与通用检测器Effort差距2.4点；复现的DeepFakesON-Phys在该数据上AUC仅0.622。
conclusion: rPPG作为检测模态能有效区分说话人脸伪造，且不同生成器的伪造难度反映其生理特性，是本文的主要理论贡献。
---

## 摘要
说话人脸（TF）深度伪造生成从静态源图像和音频信号合成逼真面部视频，产生的伪造品当前基于图像的检测器始终无法识别。与换脸操作不同，TF合成没有底层真实视频来继承生理特征，这使得远程光电容积描记术（rPPG）成为此类伪造品独具动机的检测模态。我们提出了一种检测框架，通过RhythmFormer提取每个视频的rPPG波形，并训练一组轻量级分类器来区分真实与合成的生理信号。在Celeb-DF++的TF子集上，遵循严格的主体独立协议（测试身份与训练身份完全分离）进行评估，我们的1D ResNet实现了0.806的AUC和27.8%的EER，比已发表的最佳通用检测器（Effort，ICML 2025）仅低2.4个百分点，且仅依赖生理通道。我们记录了先前代表性rPPG检测器DeepFakesON-Phys的受控复现研究，显示其在传统换脸数据上的AUC从0.999退化为Celeb-DF++ TF子集上的0.622。我们进一步表明，检测难度强烈依赖于方法：在七个TF生成器中，AUC从0.985（Real3DPortrait）到0.690（IP-LAP）不等，且该排序在所有评估协议下保持完全稳定。这种差异反映了每个生成器可解释的生理特性，而非评估噪声，构成本工作的主要理论贡献。

## Abstract
Talking-face (TF) deepfake generation synthesizes photore- alistic facial video from a static source image and an au- dio signal, producing forgeries that current image-based detectors consistently fail to identify. Unlike face-swap ma- nipulation, TF synthesis has no underlying real video from which to inherit physiological characteristics, making re- mote photoplethysmography (rPPG) a uniquely motivated detection modality for this forgery category. We propose a detection framework that extracts per-video rPPG wave- forms via RhythmFormer and trains a suite of lightweight classifiers to distinguish real from synthesized physiologi- cal signals. Evaluated on the TF subset of Celeb-DF++ un- der a strict subject-independent protocol, where test identi- ties are completely separated from training identities, our 1D ResNet achieves an AUC of 0.806 and EER of 27.8%, placing it within 2.4 points of the best published general- purpose detector (Effort, ICML 2025) while operating ex- clusively on the physiological channel. We document a con- trolled reproduction study of DeepFakesON-Phys, the rep- resentative prior rPPG detector, demonstrating degrada- tion from AUC 0.999 on legacy face-swap data to 0.622 on the TF subset of Celeb-DF++. We further show that detec- tion difficulty is strongly method-dependent: AUC ranges from 0.985 (Real3DPortrait) to 0.690 (IP-LAP) across the seven TF generators, with the ranking remaining perfectly stable across all evaluation protocols. This spread reflects an interpretable physiological property of each generator rather than evaluation noise, and constitutes the primary theoretical contribution of the work.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **研究动机**：说话人脸（Talking-Face, TF）深度伪造能从单张静态图像和音频生成逼真的面部视频。现有的图像检测器对此类伪造普遍失效，因为它们通常依赖视觉纹理或空间伪影。
*   **核心问题**：与换脸（face-swap）不同，TF 生成没有底层真实视频，因此无法继承任何真实的生理信号（如心跳引起的面部颜色变化）。这反而使得生理信号检测在此场景下具有独特的理论优势。
*   **整体含义**：本研究首次专门探索了远程光电容积描记术（rPPG）作为检测 TF 深度伪造的法医模态的可能性，并揭示了不同 TF 生成方法因其像素合成策略不同，导致伪造视频的“生理可检测性”存在固有且易解释的差异。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想**：既然 TF 生成器不刻意合成连贯的生理信号，从视频中提取的心跳波形（rPPG）在真假视频间应存在显著差异。通过训练一个轻量分类器来辨别这些波形，即可实现检测。
*   **关键技术流程（见图1）**：
    1.  **人脸检测与裁剪**：使用 MediaPipe BlazeFace 模型检测人脸，并将边界框扩大 1.5 倍以提供上下文。
    2.  **时间窗口分割**：将视频处理成固定的 160 帧片段。对于过短或过长的视频，分别采用线性插值或步幅为 60 的滑动窗口（公式 1）进行采样。
    3.  **rPPG 波形提取**：将每个 160 帧片段送入 RhythmFormer 模型，提取出对应的脉搏波形 `x ∈ R^160`。
    4.  **每窗口归一化**：为消除身份相关的幅值偏差，对每个波形独立进行 Z-score 标准化（公式 2），保留波形形状和频率信息。
    5.  **分类器架构**：训练一系列轻量级的一维模型对波形进行分类，包括：
        *   **1D ResNet**（主模型，165K 参数）：由三个阶段的残差块构成。
        *   **1D CNN** （56K 参数）：顺序卷积块。
        *   **1D Transformer**（69K 参数）：使用图像块（patch）处理和 Transformer 编码器。
*   **训练细节**：使用加权二元交叉熵损失（公式 3）缓解真实样本稀缺导致的类别不平衡（真:假 ≈ 1:7）。采用两种数据增强：添加高斯噪声和随机时间片段归零（类似 SpecAugment），防止模型过拟合训练身份的特征。

### 3. 实验设计：数据集、评测基准与对比方法

*   **数据集**：使用 **Celeb-DF++** 基准数据集中的 TF（说话人脸）子集，包含 590 段真实视频（来自 59 位名人）和 17,500 段伪造视频（由 7 种方法生成，每种 2,500 段）。
*   **评测基准与协议**：
    *   采用严格的**主体独立**（Subject-independent）数据划分：59 位名人按身份划分为训练集（41人）、验证集（9人）和测试集（9人），确保测试身份在训练中完全不可见，防止生理特征泄漏。
    *   主要评测指标为 **AUC** (ROC 曲线下面积) 和 **EER** (等错误率)。
*   **对比方法**：
    *   **先前 rPPG 检测器复现**：严格复现了 **DeepFakesON-Phys**（原换脸检测器），并评估其在 TF 数据上的性能，以量化分布偏移。
    *   **当前最优通用检测器**：将结果与 Celeb-DF++ 上的最佳通用模型 **Effort** (ICML 2025) 进行比较。
    *   **内部架构对比**：对比了提出的 1D ResNet、1D CNN 和 1D Transformer 的性能。

### 4. 资源与算力

*   论文正文及参考文献中**未明确提及**所用 GPU 的型号、数量、训练单次时长或总计算成本。

### 5. 实验数量与充分性

*   **实验数量**：论文进行了多组、多层次的实验，总计超过 75 轮训练（5折交叉验证×多个条件），设计全面且系统。
    *   **复现实验**：对 DeepFakesON-Phys 进行了完整的复现与评估。
    *   **消融/隔离实验**：评估了 Z-score 归一化和数据增强的单独效果。
    *   **主结果实验**：在 18 位测试身份上，用 5 个随机种子平均，评测了 1D ResNet 和 Transformer。
    *   **每方法分析**：对 7 种生成器逐一进行了详尽的性能分析和波形对比。
    *   **隔离训练实验**：为每个生成器单独训练和评估模型，探究单种伪造信号的“可学习性”。
*   **充分性与公平性**：实验设计**充分且公平**。5折分层分组交叉验证、严格的单独立身份划分、与多个强基准的对比以及多随机种子的平均，都确保结论的可靠性、客观性与可复现性。

### 6. 论文的主要结论与发现

*   **rPPG 模态有效性得到证实**：仅凭生理通道，1D ResNet 便取得 0.806 的 AUC，比复现的 DeepFakesON-Phys 提高 0.184，与最佳通用检测器 Effort 的差距仅 2.4 个百分点。
*   **分布偏移量化**：先前在换脸数据上表现优异的 DeepFakesON-Phys，在 TF 数据上 AUC 从 0.999 剧烈下降至 0.622，证实了 TF 检测的独有挑战和旧模型的失败。
*   **检测难度与方法强相关的核心发现**：不同 TF 生成器的可检测性差异巨大且排序稳定。
    *   **极易检测**：Real3DPortrait（AUC 0.985）、EDTalk (0.950)、SadTalker (0.946)，它们完全从潜在表示合成像素，无半点真实生理特征。
    *   **较难检测**：FLOAT (0.794)、EchoMimic (0.865)，部分保留了源帧像素，意外产生了类似 rPPG 的色彩变化。
    *   **极难检测**：IP-LAP (AUC 0.690)，其保持身份不变性的机制，意外生成了高度逼真的生理信号，使其输出与真实信号几乎无法区分。
*   **IP-LAP 的特殊性**：IP-LAP 是唯一一个在隔离训练下，模型性能反而比组合训练更差的方法，证明其 rPPG 特征本质上与真实生理信号无异，是当前生理检测的最大挑战。

### 7. 优点

*   **开辟新方向**：首次将 rPPG 检测模态专门应用于 TF 深度伪造，填补了领域空白。
*   **论证严谨**：清晰的理论论证（TF 无生理基底 -> rPPG 检测优势）与扎实的实证分析相结合。
*   **实验设计科学**：严格的身份独立协议、有效的复现研究、对旧方法的“重新基线化”值得称赞，为领域树立了可复现的范例。
*   **理论贡献深刻**：不仅提出一个检测器，更揭示了“生成方法 -> 生理可检测性”的稳定关联，为理解伪造生成器的内在属性和预测未来风险提供了可解释的框架。

### 8. 不足与局限

*   **真实身份数量受限**：整个评估仅基于 59 位名人的真实视频，测试集仅 9 人，制约了结论在更广人群生理多样性上的泛化能力。
*   **缺乏人口统计公平性评估**：rPPG 性能受肤色和光照影响，但论文并未对检测性能按人口统计属性（如肤色类型）进行分层评估，可能存在公平性风险。
*   **标签验证限制**：每方法分析中，伪造视频的方法标签依赖于数据集预设的排序，无法通过文件名独立校验。
*   **双用途风险**：详尽刻画各生成器的生理弱点（尤其是 IP-LAP 的逼真性），可能为设计对抗性样本、规避检测的合成器提供“路线图”。
*   **单一模态**：当前仅依赖 rPPG 信号，与顶尖通用检测器的微小差距，提示未来可融合视觉或频域信号来完全追平。

（完）
